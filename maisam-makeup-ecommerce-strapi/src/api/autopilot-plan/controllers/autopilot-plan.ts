/**
 * autopilot-plan controller
 */

import { createOwnerScopedController } from '../../../utils/owner-scope-controller';

export default createOwnerScopedController(
  'api::autopilot-plan.autopilot-plan',
  'users_permissions_user'
);
